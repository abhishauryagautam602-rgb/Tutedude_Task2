<>-is known as an opening tag
</>- is known as an closing tag
<h1>,<h2>,<h3>,<h4>-is used to give the header in the webpage
<section>-This is used to separate the code into different sections like booknow,Price list
<header>-This is used to separate the top section of the code
<main>-This is used to separate the main section of the code
<footer>-This is used to separate the lower section of the code
<title>-This is used to make the tritle of the webpage
<p>-This is the tag used to write paragraphs on the webpages
<img>-this is the tag used to display the images on the webpages using link through search engine .we can also define the dimensions of the images
<ol>-this is used to make ordered list in the webpages.
<li>-This is used to write elements in the list on the webpages.
<table>-This is used to make table on the webpages.
border in the table is used to define the type of border used to make a table.
<thead>-this is used to meke heading in the table
<th>-this is used to write the heading of the table
<tbody>-this is used to make visible content on the webpage.
<tfoot>-This is used to make footer content on the webpage.
<td>-This is used to write the contents on the webpage.
<form action="">-This is used to create a section of the webpage that collects user input and 
sends the it to a server via javascripts.
<input type="">-this is used to create a dropbox which helps us to input data in a specific area
and the type of input we need like-tel,email,number,etc.
<section>,<header>,<footer>,<main>-These are called semantic tags which is used to divide the code into different section without changing the appearence of the webpages.
<body>- is used to is used to contain all the visible content on a webpage.
header ,parapgraph,links,photos,etc. 
<html>- are used to contain various type of tags.
<!DOCTYPE html>-it declaration that tells the browser the document is written in HTML5.